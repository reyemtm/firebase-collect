export function dbConfig() {
  
  const fields = ["title", "notes"];
  const title = "title";
  const data = ["points", "lines"]
  const config = {
    title: title,
    fields: fields,
    data: data
  }

  return config
}

export function dbLoadProject(config) {
  
  /*
  check if project exists
  if not create project
  ping project
  return success status 
  */

  return status

}